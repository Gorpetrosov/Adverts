import type {IAdvert} from "@/types/avdert";

export interface IAdvertiseItemProp {
    advertise: IAdvert
}


export interface countryListByKeyValue {
     id: number;
     name: string;
     key: string;
}